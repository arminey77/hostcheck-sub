- name: Install pip
  ansible.builtin.apt:
    name: python3-pip
    state: present
    force_apt_get: true

- name: Install dnspython library
  ansible.builtin.pip:
    name: dnspython
    extra_args: -i {{ pip_repo }}